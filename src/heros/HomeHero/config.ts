import type { Block } from 'payload'

export const config: Block = {
  slug: 'homeHero',
  labels: {
    singular: 'Home Hero',
    plural: 'Home Heroes',
  },
  fields: [], // No configurable fields needed as everything is hardcoded
}
